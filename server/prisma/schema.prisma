generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int       @id
  userId    String
  email     String?
  phone     String?
  address   String?
  createdAt DateTime? @default(now())
}
