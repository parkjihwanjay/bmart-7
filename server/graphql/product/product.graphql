scalar Date
scalar JSON

type Product {
  id: Int!
  title: String!
  description: String!
  category: Category!
  originPrice: Int!
  salePrice: Int!
  salePercent: Int
  isMain: Boolean!
  hit: Int!
  amount: Int!
  mainImage: String!
  bannerImage : String!
}

type Query {
  getRecommended(offset:Int!, limit:Int!, categoryId:Int!): [Product!]
}

type Mutation {
  insertProducts: String
}
